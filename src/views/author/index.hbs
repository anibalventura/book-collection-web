<main>
  <div class="row justify-content-center mb-4">
    <div class="col-11">
      <div class="card shadow-sm">
        <div class="card-header bg-primary bg-opacity-75">
          <h3 class="text-white">
            Authors Maintenance
            <a
              class="btn btn-success shadow-sm"
              href="/authors/create"
            >Create</a>
          </h3>

        </div>
        {{#if hasAuthors}}
          <div class="card-body">
            <div class="row">

              {{#each authors as |author|}}
                <div class="col-3 mb-3">
                  <div class="card shadow-sm h-100">
                    <div class="card-body">

                      <h4>{{author.name}}</h4>
                      <div>
                        <div>
                          <label class="card-subtitle text-muted">Email:</label>
                          <span class="">{{author.email}}</span>
                        </div>
                        <div>
                          <label class="card-subtitle text-muted">Books:</label>
                          <span class="">{{author.count}}</span>
                        </div>
                      </div>
                      <div
                        class="mt-3 d-flex justify-content-between align-items-center"
                      >
                        <div class="btn-group">
                          <a
                            href="/authors/edit/{{author.id}}"
                            class="btn btn-sm btn-outline-warning"
                          >Edit</a>
                          <a
                            href="/authors/delete/{{author.id}}"
                            class="btn btn-sm btn-outline-danger"
                          >Delete</a>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>

              {{/each}}
            </div>
          </div>
        {{else}}
          <div class="container-fluid text-center m-5">
            <h4>No Authors created!</h4>
            <p>Start creating one.</p>
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</main>
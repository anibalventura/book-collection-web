<div class="row justify-content-center mb-4">
  <div class="col-4">
    <div class="card shadow-sm">
      <div class="card-header bg-danger text-light">
        <h4>Are you sure you want to delete the editorial
          {{editorial.name}}?</h4>
      </div>
      <div class="card-body">
        <form method="post" action="/editorials/delete">

          <input type="hidden" name="editorialId" value="{{editorial.id}}" />

          <div class="container-fluid text-center">
            <button
              type="submit"
              class="btn btn-primary shadow-sm"
            >Delete</button>
            <a href="/editorials" class="btn btn-secondary shadow-sm">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>